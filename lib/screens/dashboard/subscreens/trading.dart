import 'package:flutter/material.dart';

class Trading extends StatelessWidget {
  const Trading({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Trading'));
  }
}
